# Pair-Session I0001 — Board anlegen

- fertig-kriterium: US-1 bis US-6 aus `Anforderungen/user-stories/R00001.md` (jedes Szenario mindestens ein Test)
- wbs: Dokumentation/Planung/kanbanc.md
- knoten: I0001
- requirement: R00001
- modus: moderat
- schrittweite: Datei bzw. Methode (Default)
- autocommit: aus
- quellen: .claude/pair/quellen.md

## Entwurfsentscheidungen (geeint 2026-08-29)

- Contracts sind das Adaptermodell der JSON-Serialisierung: positional records, Listen als `IReadOnlyList<T>` — begründete Abweichung von C09/C11 nur dort; BL-intern gilt der Katalog.
- Kein `AngelegtAm` (kein Akzeptanzkriterium), kein `Spalten.Abschlussspalte()` (kein produktiver Aufrufer) — C16.
- Termine im Repository als ISO-Text im privaten Zeilenmodell (C04) → `DateOnly?` im DTO; kein `DateTime.Now` im Code.
- Zurückweisung als Contracts-DTO `Zurueckweisung(Befunde)` mit HTTP 400; `BoardArt` als String-Enum (unbekannter String → 400 durch das Framework).
- Validator kommt mit F0002; `BoardService` ruft ihn ab B0011.

## Bubble-Startzeiten

| Bubble | Start | Ende |
|---|---|---|

## Wissenslücken

(keine festgestellt)

## Ungeprüft (nur `w`)

(keine)
